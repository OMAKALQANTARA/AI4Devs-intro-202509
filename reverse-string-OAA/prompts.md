
Prompt #1
Take the role as web developer. I have a web project with a simple structure index.html and script.js on where I need to achieve the reverse string display based on any text input. So build a basic HTML code where the title is "Reverse String" then below an input text for the base text, below a blue button with the action text Reverse and include an icon for reverse, below display the result for the action and below display with the result a grey button for Copying (include a copy icon) the result. Generate the needed HTML with bootstrap and the javascript for the action separated.

Prompt #2
Now add a javascript functionality display the Reverse button only when the user has typed more than characters, also execute the reverse functionality in real time as the user types, also move the button's icon to the right side of the text. Additionaly reduce the javascript code generated by using any modern available library.

Prompt #3
Try again, the Reverse button is being displayed all the time, it shoud be hidden when input is 3 or less characteres and displayed when it is greater than 3.

Prompt #4
Still not working as expected the Reverse button is shown all the time, hide it when input has 3 or fewer characters and show it when input has more than 3 characters. 

Prompt #5
It is not working since the display style is being overriden by the higher priority display from .d-grid

Prompt #6
Format the Copy button with the same width as the Reverse button and place it's icon to the right side.

Prompt #7
Copy button is still being displayed with the full width 

Prompt #8
It is still taking the full width... the reason is the display: block !important; style applied to the Reverse button, this is making it to be displayed properly, then  add it to the Copy button too. 


